# Настройки логгера

## Обзор

Логгер теперь поддерживает гибкие настройки по уровням и модулям, а также глобальные настройки для всего проекта. **Все настройки автоматически сохраняются в файл `logger_config.json`** и загружаются при запуске приложения.

## Возможности

### 1. Настройки по модулям

Каждый модуль может иметь индивидуальные настройки:

- **Уровень логирования** (DEBUG, INFO, WARNING, ERROR, CRITICAL, SUCCESS)
- **Цветной вывод** (включен/выключен)
- **Включение/выключение** модуля

### 2. Глобальные настройки

- **Временные метки** - включить/выключить отображение времени
- **Иконки** - включить/выключить отображение иконок для уровней
- **Цвета** - включить/выключить цветной вывод
- **Имена модулей** - включить/выключить отображение имен модулей
- **Минимальный глобальный уровень** - установить минимальный уровень для всех модулей

### 3. Автоматическое сохранение

- Все изменения настроек автоматически сохраняются в файл `logger_config.json`
- При запуске приложения настройки автоматически загружаются из файла
- Возможность сброса к значениям по умолчанию

## API эндпоинты

### Получить статус всех логгеров
```
GET /api/logger/status
```

### Установить уровень логирования для модуля
```
POST /api/logger/module/level
{
  "module": "HA-Manager",
  "level": "DEBUG"
}
```

### Включить/выключить цвета для модуля
```
POST /api/logger/module/colors
{
  "module": "MyHome",
  "enabled": false
}
```

### Включить/выключить логирование для модуля
```
POST /api/logger/module/enabled
{
  "module": "Database",
  "enabled": false
}
```

### Установить глобальную настройку
```
POST /api/logger/global/setting
{
  "setting": "enable_timestamps",
  "value": false
}
```

### Сбросить конфигурацию к значениям по умолчанию
```
POST /api/logger/reset
```

## Программное управление

### Импорт функций
```python
from utils.logger import (
    set_module_level, set_module_colors, set_module_enabled,
    set_global_setting, get_logger_status, print_logger_status,
    reset_logger_config, get_logger_config, LogLevel
)
```

### Примеры использования

```python
# Установить уровень DEBUG для модуля HA-Manager
set_module_level("HA-Manager", LogLevel.DEBUG)

# Отключить цвета для модуля Database
set_module_colors("Database", False)

# Отключить логирование для модуля WebSocket
set_module_enabled("WebSocket", False)

# Отключить временные метки глобально
set_global_setting("enable_timestamps", False)

# Отключить иконки глобально
set_global_setting("enable_icons", False)

# Получить статус всех логгеров
status = get_logger_status()

# Вывести статус в консоль
print_logger_status()

# Сбросить конфигурацию к значениям по умолчанию
reset_logger_config()

# Получить объект конфигурации для прямого доступа
config = get_logger_config()
print(f"Config file: {config.CONFIG_FILE}")
```

## Доступные модули

- **HA-Manager** - управление Home Assistant
- **MyHome** - управление устройствами MyHome
- **Database** - работа с базой данных
- **WebSocket** - WebSocket соединения
- **API** - веб-API
- **Config** - конфигурация
- **Device** - клиенты устройств
- **Port** - порты устройств
- **GoogleConnector** - интеграция с Google Sheets
- **Singleton** - паттерн Singleton

## Уровни логирования

- **DEBUG** 🔍 - отладочная информация
- **INFO** ℹ️ - информационные сообщения
- **WARNING** ⚠️ - предупреждения
- **ERROR** ❌ - ошибки
- **CRITICAL** 🚨 - критические ошибки
- **SUCCESS** ✅ - успешные операции

## Глобальные настройки

- **enable_timestamps** - включить/выключить временные метки
- **enable_icons** - включить/выключить иконки
- **enable_colors** - включить/выключить цвета
- **show_module_names** - включить/выключить имена модулей
- **min_global_level** - минимальный глобальный уровень

## Примеры конфигурации

### Для разработки (много отладочной информации)
```python
set_module_level("HA-Manager", LogLevel.DEBUG)
set_module_level("MyHome", LogLevel.DEBUG)
set_module_level("WebSocket", LogLevel.DEBUG)
set_global_setting("enable_timestamps", True)
set_global_setting("enable_icons", True)
```

### Для продакшена (только важные сообщения)
```python
set_module_level("HA-Manager", LogLevel.WARNING)
set_module_level("MyHome", LogLevel.INFO)
set_module_level("Database", LogLevel.ERROR)
set_module_enabled("WebSocket", False)
set_global_setting("enable_timestamps", True)
set_global_setting("enable_icons", False)
```

### Для отладки конкретного модуля
```python
# Отключить все модули кроме HA-Manager
for module in ["MyHome", "Database", "WebSocket", "API", "Config", "Device", "Port"]:
    set_module_enabled(module, False)

# Включить только HA-Manager с DEBUG уровнем
set_module_enabled("HA-Manager", True)
set_module_level("HA-Manager", LogLevel.DEBUG)
```
